server:
  port: 59423
  servlet:
    charset: UTF-8
    enabled: true
    force: true

spring:
  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:mem:concertdb;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
    username: sa
    password:

  jpa:
    hibernate:
      ddl-auto: create        # ?? ??? update, ?? ?????? validate ?? none ??
    properties:
      hibernate:
        format_sql: true
        show_sql: true
    open-in-view: false

  h2:
    console:
      enabled: true           # H2 ? ?? ???
      path: /h2-console       # ?? ?? ??

logging:
  level:
    org.hibernate.SQL: debug
    org.hibernate.type.descriptor.sql.BasicBinder: trace  # ??? ??? ?? ??